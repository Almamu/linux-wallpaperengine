#pragma once

namespace WallpaperEngine::WebBrowser {
/**
 * Simple helper app that is used to run the web browser subprocesses
 * instead of the original binary (to prevent frontends from needing to implement this)
 */
class WebBrowserHelper { };
}